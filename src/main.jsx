import React from 'react'
import ReactDOM from 'react-dom/client'
import AudioMasteringStudio from './AudioMasteringStudio.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AudioMasteringStudio />
  </React.StrictMode>,
)